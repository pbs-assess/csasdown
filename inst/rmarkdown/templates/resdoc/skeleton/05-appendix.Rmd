# APPENDIX A. BIOLOGICAL DATA

```{r setup-appendix-a}
# Set the current appendix letter and configure automatic figure/table numbering
set_appendix("A")
appendix_fig_opts()  # Configures figures to number as A.1, A.2, etc.
appendix_tab_opts()  # Configures tables to number as A.1, A.2, etc.
```

This appendix contains biological data and demonstrates appendix-specific figure and table numbering (A.1, A.2, etc.).

## Sample Biological Data

```{r fig-biology-example, fig.cap="Example biological data plot"}
# Create a simple example plot
set.seed(123)
length_data <- data.frame(
  length_cm = rnorm(100, mean = 45, sd = 10),
  weight_kg = rnorm(100, mean = 2.5, sd = 0.8)
)

plot(length_data$length_cm, length_data$weight_kg,
     xlab = "Length (cm)", ylab = "Weight (kg)",
     pch = 16, col = rgb(0, 0, 1, 0.5))
```

You can also create tables with appendix numbering:

```{r tab-biology-summary, tab.cap="Summary statistics for biological data"}
summary_table <- data.frame(
  Statistic = c("Mean Length", "SD Length", "Mean Weight", "SD Weight"),
  Value = c(
    round(mean(length_data$length_cm), 2),
    round(sd(length_data$length_cm), 2),
    round(mean(length_data$weight_kg), 2),
    round(sd(length_data$weight_kg), 2)
  )
)

flextable::flextable(summary_table)
```

## Subsection Example

You can have subsections within appendices as usual. The figure numbering continues within the same appendix (A.1, A.2, A.3...).

```{r fig-biology-histogram, fig.cap="Length frequency distribution"}
hist(length_data$length_cm,
     main = "",
     xlab = "Length (cm)",
     col = "lightblue",
     border = "white")
```


# APPENDIX B. ADDITIONAL ANALYSES

```{r setup-appendix-b}
# Reset numbering for Appendix B (B.1, B.2, etc.)
set_appendix("B")
appendix_fig_opts()
appendix_tab_opts()
```

This appendix demonstrates that figure and table numbering restarts for each appendix.

## Model Results

```{r fig-model-diagnostic, fig.cap="Model diagnostic plot"}
# Create example diagnostic plot
set.seed(456)
residuals <- rnorm(50, 0, 1)
fitted <- seq(1, 50)

plot(fitted, residuals,
     xlab = "Fitted values", ylab = "Residuals",
     pch = 16, col = rgb(1, 0, 0, 0.5))
abline(h = 0, lty = 2)
```

```{r tab-model-parameters, tab.cap="Model parameter estimates"}
model_params <- data.frame(
  Parameter = c("Intercept", "Slope", "R²"),
  Estimate = c(1.23, 0.45, 0.78),
  SE = c(0.12, 0.05, NA)
)

flextable::flextable(model_params)
```

# APPENDIX C. SUPPLEMENTARY FIGURES {-}

```{r setup-appendix-c}
set_appendix("C")
appendix_fig_opts()
appendix_tab_opts()
```

Additional supplementary figures can go here.

```{r fig-supplementary, fig.cap="Supplementary analysis results"}
set.seed(789)
barplot(rnorm(5, 10, 2),
        names.arg = LETTERS[1:5],
        xlab = "Category",
        ylab = "Value",
        col = "steelblue")
```
